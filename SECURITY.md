# Security Policy

## 支持范围

安全修复优先覆盖 main 分支和 README 标记的当前本地版本。历史实验 checkpoint 用于证据复核，通常不会单独维护。

MyAI 当前是回环地址上的单机个人智能体 Demo，不是公网服务或多租户系统。以下行为不属于支持范围：

- 将 Java 或 Python 端口暴露到 LAN/公网；
- 绕过 Java 直接让不可信客户端访问 Python Agent；
- 在共享或不受信任的操作系统账户下保存敏感对话、记忆或文档；
- 启用未审计的写入型工具；
- 把 include_sensitive=true 的诊断响应转发给第三方。

## 当前信任边界

- Java 与 Python 默认绑定 127.0.0.1；
- Java 负责认证、CSRF、用户和会话归属；
- Python 服务目前没有独立的 Java-to-Python 服务身份认证，因此只能保留在可信本机；
- SQLite、Chroma、日志、上传文档和运行记录默认未加密，依赖本机账户和文件权限；
- 内置文件系统/Git 连接器为只读 MCP-style 适配器。路径过滤和策略不是操作系统沙箱；
- provider 端点默认应为回环地址。改用远程端点意味着提示、记忆或文档片段可能离开本机，需要单独评估；
- 便携包的 manifest 和 SHA-256 用于完整性校验，不等同于数字签名或发布者身份认证。

更多限制见 [Known Limitations](docs/KNOWN_LIMITATIONS.md)。

## 报告漏洞

请优先使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 私密报告渠道，并提供：

- 受影响版本或 commit；
- 可复现的最小步骤；
- 实际影响与前置条件；
- 已验证的缓解措施；
- 不包含真实用户数据的合成日志或截图。

如果私密漏洞报告功能尚未启用，请只创建一个不含技术细节的公开 Issue，请求维护者开启私密沟通渠道。不要在公开 Issue、Discussion、PR 或演示视频中披露可利用细节、密钥、个人数据或未脱敏日志。

项目当前不承诺正式响应 SLA 或漏洞赏金。维护者会在可行范围内确认、分级、修复并在安全版本可用后协调披露。

## 安全测试规则

- 只测试你拥有或明确获准的本地环境；
- 使用合成用户、文档、记忆和凭据；
- 不访问第三方账户、服务或数据；
- 不以压力测试导致数据丢失或服务不可用；
- 不提交包含攻击载荷生成的数据库、日志或缓存；
- 发现可能真实有效的秘密后立即停止使用，通知所有者轮换，并在发布前清理 Git 可达历史。

## 凭据与敏感数据处理

- 不要提交 .env、config/app.env、scripts/start-myai.env、数据库、日志、模型或真实上传内容；
- API key 和密码应通过本地忽略文件或进程环境注入；
- 示例值必须明显是测试占位符，不能由真实值变形或截取；
- 若秘密曾进入 Git，仅删除当前文件不够：应先轮换，再确保待推送分支的完整可达历史已清理；
- 公开诊断材料前，检查用户名、绝对路径、令牌、cookie、会话、文档内容和模型服务地址。

## 部署建议

当前推荐部署方式只有受信任个人电脑上的 loopback-only 本地模式。需要 LAN、公网、团队共享、容器编排或云部署时，应先补齐服务间认证、TLS、集中密钥管理、加密与保留策略、耐久队列、多实例协调、审计导出和灾难恢复；现有 Demo 配置不可直接视为这些场景的安全基线。
